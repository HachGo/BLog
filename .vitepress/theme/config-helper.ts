import { type App } from 'vue'

/**
 * 应用增强配置
 */
export function enhanceApp({ app }: { app: App }) {
  // 注册全局组件（如果需要）
  // app.component('MyComponent', MyComponent)

  // 配置路由选项
  // app.config.globalProperties.$someKey = 'someValue'
}
